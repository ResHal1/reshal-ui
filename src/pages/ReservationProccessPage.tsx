import React from "react";
import Menu from "../components/Menu";

const ReservationProccessPage = () => {
  return (
    <div>
      <Menu />
    </div>
  );
};

export default ReservationProccessPage;
